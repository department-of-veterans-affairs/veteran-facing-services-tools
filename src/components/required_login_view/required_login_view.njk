import React from 'react';
import Modal from './required_login_view';

export default function ModalExample(props) {
  return (
    <Modal
      title={props.title}
      id={props.id}
      visible
      onClose={() => {}}>
      {props.content}
    </Modal>
  );
}
