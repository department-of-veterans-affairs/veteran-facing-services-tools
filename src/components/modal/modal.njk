import React from 'react';
import Modal from './Modal';

export default function ModalExample(props) {
  return (
    <Modal
      title={props.title}
      id={props.id}
      visible>
      {props.content}
    </Modal>
  );
}
