import React from 'react';
import ErrorableCheckbox from '../errorable_checkbox/ErrorableCheckbox';

export default function ErrorableCheckboxExample(props) {
  return (
          <ErrorableCheckbox
            onValueChange={() => {}}
            {...props}/>
  );
}
